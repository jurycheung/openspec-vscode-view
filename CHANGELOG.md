# Changelog

## 0.1.3（2026-09-02）

修复与刷新链路稳定性专项。

- 修复：删除被扫描的项目根目录后，树视图仍残留该项目与旧变更——每次刷新前对账根集合（已删/被移走的根自动移除，新根自动出现），同时覆盖手编 `~/.sef/config.json` 的场景
- 修复：点击树节点偶发报错 "Actual command not found"——TreeItem 命令不再携带 `arguments`，避开 VSCode 委托命令缓存在树刷新时的失效竞态；点击上下文改由 `treeView.selection` 解析
- 改进：树视图只在数据真正变化时刷新，消除周期对账带来的树无效化抖动
- 修复：树中点击输出件忽略 `openBeside` 设置的问题；修正 `setModels` 对旧模型的重复订阅

## 0.1.2（2026-09-02）

改进：手动刷新按钮增加视图内进度指示（`withProgress`），刷新过程在树标题区可见，不再只有一闪而过的状态栏消息。

- 实测确认刷新数据管线正常（新建/删除 change 目录后一次刷新即反映最新状态）；本版本解决的是「数据已最新时按钮看起来无效」的感知问题

## 0.1.1（2026-09-02）

修复：删除 change 目录后，树视图不自动移除该变更（个别文件监听后端不产生目录删除事件）。

- 文件监听补充 `openspec/changes`、`openspec/changes/*`、`openspec/schemas/*` 三种模式，覆盖不同后端的事件上报路径
- 窗口重新聚焦、树视图重新可见时自动对账刷新
- 新增设置 `openspec-vscode-view.reconcileInterval`：周期性状态对账（默认 15 秒，窗口聚焦时执行，0 关闭），兜底一切漏报场景

## 0.1.0（2026-09-01）

首个公开发布版本。

- 自动检测 openspec 项目：工作区 folders、`${HOME}/openspec`、`~/.sef/config.json` 配置的额外扫描路径（树视图标题栏 ⚙ 按钮管理，首次创建时预置工作区与主目录作为示例）
- 目录树按 change → 阶段 → 输出件分层展示，输出件点击直达目标文件
- 流程可视化面板：垂直流水线、当前阶段高亮、依赖链着色、输出件点击旁侧打开、VSCode 主题自适应
- 自定义 schema 支持：解析 `openspec/schemas/<name>/schema.yaml` 的阶段定义与描述
- 状态数据源：`openspec status --all --json`（CLI 未安装 / 解析失败 / 部分加载失败均有容错展示）
- 当前阶段判定：最后一个已完成阶段的后继；被流程越过的中间阶段显示为跳过；「▶ 当前阶段」为唯一边界点，就绪阶段显示「可开始」
- 首次加载前显示「正在扫描中…」；文件监听自动刷新（防抖）；多根工作区支持
